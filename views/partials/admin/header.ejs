<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>FootChic</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
<head>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">

  <!-- Font Awesome CDN -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>

  <!-- Bootstrap CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    body {
      background-color: #f8f9fa;
    }
    .navbar {
      background-color: #000;
    }
    .navbar-brand {
      color: #fff !important;
      font-weight: bold;
    }
    .navbar .btn {
      background-color: #fff;
      color: #000;
      border-radius: 20px;
    }
    .table th {
      background-color: #000;
      color: #fff;
    }
    .table td {
      vertical-align: middle;
      border: none;
    }
    .search-bar {
      max-width: 250px;
    }
    /* Sidebar */
    .sidebar {
      height: 100vh;
      width: 200px;
      position: fixed;
      top: 0;
      left: 0;
      background-color: #111;
      padding-top: 60px;
    }
    .sidebar a {
      padding: 12px 15px;
      text-decoration: none;
      font-size: 16px;
      color: #ddd;
      display: block;
    }
    .sidebar a:hover {
      background-color: #575757;
      color: #fff;
    }
    .content {
      margin-left: 200px;
      padding: 20px;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
<nav class="navbar px-3 fixed-top bg-dark">
  <a class="navbar-brand fw-bold text-white" href="#">FootChic</a>
 
</nav>

  <!-- Sidebar -->
  <div class="sidebar">
    <a href="/admin/dashboard">Dashboard</a>
    <a href="/admin/users">Customers</a>
    <a href="/admin/category">Categories</a>
    <a href="/admin/products">Products</a>
    <a href="/admin/logout">Logout</a>
  </div>

  <!-- Page Content -->
  <div class="content pt-5 mt-3">
